import styled from 'styled-components';

const StarsStyle = styled.div`
    display: flex;
    flex-direction: row;
    flex: 1;
    .star{
        cursor: pointer;
    }
`;

export default StarsStyle;